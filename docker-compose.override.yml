services:
  app:
    build:
      target: development
    container_name: arcade-api-dev
    restart: "no"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --log-level debug
    volumes:
      - ./app:/app/app
